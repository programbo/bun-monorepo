{
  "name": "@bun-monorepo-template/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --hot src/index.ts",
    "start": "NODE_ENV=production bun src/index.ts",
    "build": "bun run build.ts",
    "lint": "bun run --cwd ../../ lint:all",
    "format": "bun run --cwd ../../ format:all",
    "typecheck": "bun run --cwd ../../ typecheck:all",
    "qa": "bun run --cwd ../../ qa",
    "lint:pkg": "oxlint --config oxlint.json --fix .",
    "format:pkg": "prettier --config prettier.config.cjs --write .",
    "typecheck:pkg": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "bun-plugin-tailwind": "^0.1.2",
    "react": "^19",
    "react-dom": "^19",
    "tailwindcss": "^4.1.11"
  },
  "devDependencies": {
    "@bun-monorepo-template/qa": "workspace:*",
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "@types/bun": "latest"
  }
}
