{
  "name": "bun-monorepo-template",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n web,packages \"bun run --filter web dev\" \"bun run --filter @repo/qa build -- --watch\"",
    "build": "bun run --filter @repo/qa build && bun run --filter web build",
    "lint": "bun run --filter @repo/qa lint && bun run --filter web lint",
    "typecheck": "bun run --filter @repo/qa typecheck && bun run --filter web typecheck",
    "format": "bun run --filter @repo/qa format && bun run --filter web format",
    "qa": "bun run lint && bun run typecheck && bun run format"
  },
  "devDependencies": {
    "@trivago/prettier-plugin-sort-imports": "latest",
    "concurrently": "latest",
    "oxlint": "latest",
    "prettier": "latest",
    "prettier-plugin-tailwindcss": "latest",
    "typescript": "latest"
  }
}
